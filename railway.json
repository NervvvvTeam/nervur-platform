{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "npm run build && cd server && npm install && node index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
